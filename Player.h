#include <iostream>
#include <Position.h>
#include <Inventory.h>

class Player
{
    Public:
        getPosition();
        getInventory();
        setPosition(Position);
    Private:
        Position playerPosition;
        Inventory PlayerInventory;
}
